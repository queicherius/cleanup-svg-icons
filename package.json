{
  "name": "cleanup-svg-icons",
  "version": "1.0.4",
  "description": "Clean up SVGs to be actually usable as icons",
  "bin": {
    "cleanup-svg-icons": "src/bin.js"
  },
  "scripts": {
    "test": "jest --coverage && standard"
  },
  "author": "queicherius@gmail.com",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/queicherius/cleanup-svg-icons.git"
  },
  "dependencies": {
    "execall": "^1.0.0",
    "fs-extra": "^7.0.0",
    "glob": "^7.1.2",
    "randomstring": "^1.1.5",
    "svgo": "^1.1.1"
  },
  "devDependencies": {
    "jest": "^23.6.0",
    "standard": "^14.0.0"
  }
}
